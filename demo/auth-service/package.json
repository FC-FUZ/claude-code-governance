{
  "name": "auth-service-demo",
  "version": "1.0.0",
  "description": "Demo auth service with planted bug for governance framework recording",
  "scripts": {
    "test": "node --test src/**/*.test.ts",
    "start": "node src/index.ts"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.0",
    "express": "^4.18.0"
  }
}
